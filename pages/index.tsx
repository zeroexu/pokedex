import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => (
  <Head>
    <title>Pokedex</title>
    <meta name="description" content="Pokedex - Zuata Region" />
    <link rel="icon" href="/favicon.ico" />
  </Head>
);

export default Home;
